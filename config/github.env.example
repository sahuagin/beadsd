# Map each centralized project to the GitHub repo whose issues mirror its beads.
# Install at ~/.config/beads/github.env. Used by beads-gh-sync (outbound) and
# beads-gh-triage (inbound). One `project=owner/repo` per line.
mu=sahuagin/mu
agent_tools=sahuagin/agent_tools
